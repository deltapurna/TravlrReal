journals = $('.journals');
journals.prepend("<%= escape_javascript(render('journals/journal', journal: @journal)) %>");
journals.children().last().remove();
$('#journal_title').val('');
$('#journal_content').val('');
